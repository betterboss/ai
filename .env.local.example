# =============================================================================
# Better Boss AI Platform — Environment Variables
# Copy this file to .env.local and fill in your values
# =============================================================================

# -----------------------------------------------------------------------------
# DATABASE (Required)
# Auto-injected by Vercel when you connect a Neon Postgres database
# -----------------------------------------------------------------------------
DATABASE_URL=postgresql://user:pass@ep-xxx.us-east-2.aws.neon.tech/neondb?sslmode=require

# -----------------------------------------------------------------------------
# AUTHENTICATION — NextAuth.js (Required for auth features)
# -----------------------------------------------------------------------------
# Generate with: openssl rand -base64 32
# NEXTAUTH_SECRET=
# Set to your deployment URL (e.g., https://your-app.vercel.app)
# NEXTAUTH_URL=http://localhost:3000

# Google OAuth (Optional — enables "Sign in with Google")
# GOOGLE_CLIENT_ID=
# GOOGLE_CLIENT_SECRET=

# Email/Magic Link Provider (Optional — enables passwordless email login)
# EMAIL_SERVER_HOST=
# EMAIL_SERVER_PORT=587
# EMAIL_SERVER_USER=
# EMAIL_SERVER_PASSWORD=
# EMAIL_FROM=noreply@yourdomain.com

# -----------------------------------------------------------------------------
# AI SERVICES (Required for AI features)
# Note: Users can also set their own Anthropic key via the setup page
# (stored in browser localStorage, not server-side)
# -----------------------------------------------------------------------------
# Anthropic API key — server-side fallback when user hasn't set their own
# ANTHROPIC_API_KEY=sk-ant-api03-...

# OpenAI API key — used for Whisper voice transcription and RAG embeddings
# OPENAI_API_KEY=sk-...

# -----------------------------------------------------------------------------
# EMAIL — SendGrid (Optional — enables email sequences & notifications)
# -----------------------------------------------------------------------------
# SENDGRID_API_KEY=
# SENDGRID_FROM_EMAIL=noreply@better-boss.ai

# -----------------------------------------------------------------------------
# SMS — Twilio (Optional — enables SMS sequences & AI agent via text)
# -----------------------------------------------------------------------------
# TWILIO_ACCOUNT_SID=
# TWILIO_AUTH_TOKEN=
# TWILIO_PHONE_NUMBER=

# -----------------------------------------------------------------------------
# INTEGRATIONS (Optional)
# -----------------------------------------------------------------------------
# Slack bot token — for Slack agent integration
# SLACK_BOT_TOKEN=

# GoHighLevel API key — for CRM bridge
# GHL_API_KEY=

# -----------------------------------------------------------------------------
# CRON SECURITY (Required in production)
# Vercel sets this automatically for cron jobs.
# In dev, cron endpoints are accessible without this.
# Generate with: openssl rand -hex 32
# -----------------------------------------------------------------------------
# CRON_SECRET=
